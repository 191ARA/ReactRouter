import React from 'react';
import './Home.css';

const Home = () => {
  return (
    <div className="home">
      <h1>Welcome to React Router</h1>
      <p>The routes project is a small web application developed using the React library and routing via React Router. The main goal of the project is to demonstrate skills in navigating between pages, managing application status, and interacting with the API.</p>
    </div>
  );
};

export default Home;